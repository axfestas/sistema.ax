# Backend (NestJS)

Stack sugerido: NestJS + TypeORM/Prisma + PostgreSQL

Endpoints iniciais:
- `GET /items`
- `GET /items/:id`
- `POST /reservations`
- `GET /reservations`

Comandos iniciais:
- Instalar: `npm install`
- Rodar dev: `npm run start:dev`

Autenticação: JWT com roles (admin/cliente)
